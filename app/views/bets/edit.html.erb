<h1>Editar Palpite</h1>
<p>Você está editando um palpite para o jogo <%= @match.team_a.name %> vs <%= @match.team_b.name %></p>
<p>Você pode alterar o seu palpite abaixo:</p>

<%= form_with(model: [@match, @bet], local: true) do |form| %>
  <% if @bet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bet.errors.count, "erro") %> proibiu que este palpite fosse salvo:</h2>
      <ul>
        <% @bet.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :guess_a, "Seu palpite para #{@match.team_a.name}" %>
    <%= form.number_field :guess_a, min: 0 %>
  </div>

  <div class="field">
    <%= form.label :guess_b, "Seu palpite para #{@match.team_b.name}" %>
    <%= form.number_field :guess_b, min: 0 %>
  </div>

  <div class="actions">
    <%= form.submit "Salvar Palpite" %>
  </div>
<% end %>

<p>Você pode voltar para a página inicial clicando <%= link_to "aqui", root_path %>.</p>
<p>Você pode voltar para a página de palpites clicando <%= link_to "aqui", matches_path %>.</p>